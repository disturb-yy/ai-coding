{
  "skill": "workflow-skill-gen",
  "config": {
    "target_phase": "Implementation",
    "preserve_existing_resources": true,
    "generate_openai_metadata": true,
    "strict_context_rules": true,
    "output_mode": "files"
  },
  "input": {
    "existing_skill_path": "skills/coding",
    "existing_skill_summary": "Implements code changes from a request.",
    "target_artifact_type": "implementation_result",
    "workflow_constraints": [
      "Skill must not control workflow phase transitions.",
      "Skill must output a JSON artifact envelope.",
      "Skill must work in Codex direct mode and OpenCode delegated mode."
    ]
  }
}
